{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "make_tensor_test",
            "type": "shell",
            "options": {
                "cwd": "${workspaceRoot}/build"
            },
            "command": "cmake -DCMAKE_BUILD_TYPE=Debug ${workspaceRoot}; make tensor_test",
        },
    ]
}